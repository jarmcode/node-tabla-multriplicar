# Notas:

Primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Listar la tabla de multiplicar       [boolean] [default: false]
  -h, --hasta    Número hasta el cuál se realizará la multiplicación.
                                                          [number] [default: 10]

```
